import { delay } from "$util/time"


export const fetchRecipientLocation = async (assignedId) => {
   window.open('https://webapps.doc.state.nc.us/opi/offendersearch.do?method=view')
      // let displayTable = govWindow.document.getElementsByClassName("displaytable")
      // console.log("got table")
      // console.log(displayTable)
   
      // let idField = displayTable[0].children[0].children[3].children[1].children[0] as HTMLElement
      // idField.value = assignedId
      // let submitButton = displayTable[0].children[0].children[9].children[0].children[0].children[0].children[0].children[0].children[0] as HTMLElement
      // submitButton.click()
   
   }
      
         
    
   
